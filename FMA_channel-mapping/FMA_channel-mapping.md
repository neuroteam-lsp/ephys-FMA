The CSV file <FMA_channel-mapping.csv> maps FMA physical electrodes to headstage channels for easily displaying them in a figure with physical placement.



A PDF (FMA_channel-mapping.pdf) also redundantly describes the mapping from FMA physical electrodes to headstage channels.

